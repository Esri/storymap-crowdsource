import actionTypes from 'babel/constants/ActionTypes';
import events from 'babel/constants/Events';
import components from 'babel/constants/Components';

export const ActionTypes = actionTypes;
export const Events = events;
export const Components = components;
export default {
  ActionTypes,
  Components,
  Events
};
